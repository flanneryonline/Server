#!/bin/sh

set -o errexit
set -o nounset

function jail_init {

local jail_dir=${JAIL_DIR:-${jail_dir:-"/usr/local/jails"}}
local jail_name=${1:-}
jail_root="${jail_dir}/mount/${jail_name}"


return 0
}

function download_init {

return 0
}

